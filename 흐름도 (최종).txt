┌───────────────────────────┐
│ run_giwanos_master_loop.py│  ← 마스터: 데이터 수집·정리·보고서 생성
└─────────────┬─────────────┘
              │
              │  ① 보고서(.md/.json 등) 생성
              │  ② 디스패치 티켓 JSON 생성
              ▼
        data/reports/_dispatch/*.json     ← 마스터가 만드는 표준 티켓
        data/dispatch/_queue/*.json       ← 외부/수동 큐(옵션)

              │
              ▼
┌───────────────────────────┐
│ velos_bridge.py           │  ← 브릿지: 티켓 소비 후 채널 전송
│  • Slack / Notion / …     │
│  • 성공≥1 → OK 이동       │
│  • 전송시도없음 → OK      │
│  • 모두실패 → FAILED      │
└──────┬───────────┬────────┘
       │           │
       ▼           ▼
  *_processed   *_failed           ← 처리 결과 보관
