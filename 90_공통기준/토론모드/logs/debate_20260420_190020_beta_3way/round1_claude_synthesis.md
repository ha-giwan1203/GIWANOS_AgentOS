# Round 1 — Claude 종합 설계안 (Step 6-5)

## 하네스 라벨링 (독립 분석)

### GPT 주장 4건
1. 6-2/6-4는 1줄 판정이라 원문 payload가 누적 맥락보다 중요 — **실증됨** (현행 SKILL이 이미 원문 전체 동봉 강제)
2. 라운드당 16~30초 절감 추정은 타당 — **실증됨** (세션84 진입 캐시 7→3 감축 데이터 기반)
3. 리스크 순위 b(키/비용/fallback) > a(드리프트) > c(로그) — **일반론** (규정 연계 논거만 제시, 실측 없음)
4. 안-C 선호 — **실증됨** (UI 맥락과 속도 최적의 절충)

### Gemini 주장 4건
1. 1줄 검증은 누적 맥락 의존도 낮음 — **실증됨** (토론 포맷상 자명)
2. 라운드당 16~30초 감축 추정 타당 — **실증됨** (DOM/타이핑/polling 오버헤드 기반)
3. 리스크 순위 c(로그 보존성) > a > b — **일반론** (가시 증거 단절 우려, 실측 없음)
4. **신규 요구: 로그 브릿지 파이프라인** (API 응답을 6-5 전 웹 UI 프롬프트로 주입) — **과잉설계 vs 합리적 보완** 중간 — Claude 채택 판단: 합리적 보완

### 판정
- **채택 6건**: GPT 1·2·4, Gemini 1·2, **신규 요구 (로그 브릿지)**
- **보류 2건**: GPT 3·Gemini 3 (리스크 순위 상호 대립 — 양측 관점 모두 타당, 실증 근거 부족으로 순위 고정 보류)
- **버림 0건**

## 통합 설계안 (Claude 종합)

### β안-C (통합 채택) — 6-2/6-4 API 병렬 전환 + 원문 payload + 로그 브릿지

#### 1. 규정 개정 범위 (`90_공통기준/토론모드/CLAUDE.md`)
현행 `[NEVER] API 사용 금지 — 브라우저 화면 텍스트 직접 읽기만` 조항에 **유일 예외** 신설:

> **[예외 — 단발 교차 검증만]** Step 6-2 / 6-4 교차 검증 1줄 판정에 한해 API 호출 허용. 본론(6-1/6-3), 종합(6-5), 최종 판정은 여전히 웹 UI 멀티턴. 예외 범위 확대 금지.

#### 2. 기술 구현 (`debate-mode/SKILL.md` Step 6-2/6-4 재정의)
- **병렬**: 6-2(Gemini→GPT 검증)와 6-4(GPT→Gemini 검증)를 API 병렬 호출
- **원문 payload 동봉**: 검증 대상 원문 전체 + 판정 기준 프롬프트 포함 (현행 SKILL.md Step 3-W 146~151 계승)
- **모델 매칭**: 본론 GPT 웹 UI 모델과 동일 프로바이더 API 사용(OpenAI), Gemini 측도 동일(Google). 모델 드리프트 최소화
- **실패 fallback**: API 호출 실패 시 기존 웹 UI 경로로 자동 복귀 (1회 재시도 후)
- **재사용 경로**: 세션83 `90_공통기준/토론모드/openai/openai_debate.py` 리팩터 + Gemini API 클라이언트 신설

#### 3. 로그 브릿지 파이프라인 (Gemini 신규 요구 채택)
6-5 Claude 종합 시작 전 아래 JSON을 웹 UI 프롬프트에 주입:
```json
{
  "cross_verification": {
    "gemini_verifies_gpt": {"verdict": "동의", "reason": "..."},
    "gpt_verifies_gemini": {"verdict": "동의", "reason": "..."}
  },
  "log_path": "90_공통기준/토론모드/logs/debate_YYYYMMDD_*/round*_cross_verification.md"
}
```
- 저장: `logs/debate_*/roundN_cross_verification.json` (기존 md와 이중화)
- 주입: 6-5 첫 메시지에 "6-2/6-4 검증 결과 원문 첨부" 고정 prefix

#### 4. 리스크 통제 (쟁점 3 양측 관점 모두 수용)
- (b) 키 관리/비용/fallback (GPT 강조): 키는 세션별 발급·종료 시 revoke 정책 고정, OpenAI/Gemini 각 월별 예산 상한 설정, 실패 fallback 웹 UI 복귀 명시
- (c) 로그 보존성 (Gemini 강조): md + JSON 이중 기록, 6-5 프롬프트에 원문 주입으로 가시 증거 보존
- (a) 모델 드리프트: 본론과 동일 프로바이더 API 사용 + 모델 버전 로그 고정

#### 5. 검증 체크리스트
- [ ] API 키 관리 프로세스 (발급·보관·revoke) 문서화
- [ ] `openai_debate.py` + Gemini API 클라이언트 각 스크립트 구현
- [ ] 로그 브릿지 JSON 스키마 + 주입 로직 구현
- [ ] smoke_test 신규 섹션 (API 실패 fallback, 원문 payload, 로그 주입)
- [ ] 규정 개정 후 2주 관찰 기간 설정, incident 0건 확인 후 고정

## 보류 2건 처리
쟁점 3 리스크 순위 대립은 **우열 판정 없이 양측 관점 모두 설계에 수용**. GPT의 (b) 키/비용 우선 관점은 "키 관리 프로세스 문서화 + 예산 상한 + fallback 자동 복귀"로 반영. Gemini의 (c) 로그 보존성 우선 관점은 "로그 브릿지 JSON 주입"으로 반영. 상호 보완적 보강.

## 시스템 제약 자기 검사
- `round_count` = 1 (현 라운드)
- `issue_class` = **B** (규정 개정 포함 프로토콜 변경 → 6-5 유지 필수)
- `skip_65` = false
- `claude_delta` = "mapping_and_integration" (로그 브릿지 채택 + 리스크 보강 = 순수 축약 아님)

## 양측 1줄 검증 요청 (Step 6-5 완성)
양측에 본 종합 설계안 원문 전체 동봉 → `동의 / 이의 / 검증 필요` + 근거 1문장.
