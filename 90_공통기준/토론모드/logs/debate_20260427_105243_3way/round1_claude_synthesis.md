# Round 1 — Claude 종합안 (Step 6-5)

## 합의 채택안 (양측 1줄 검증 PASS 후)

### 메타 의제0 — 해석 C 채택
**해석 C: 의제 재정의** — incident 누적이 실무 산출물 작업을 막는 "실제 막힘"이면 세션108 단서("실제로 막히는 오류 1건만 최소 수정")에 부합. P2-B(35건 누적)만 토론 대상이고 P2-C·P3-E는 보류.

**Gemini 보강 채택**: P2-B 1건은 "예외적 허용"이 아니라 "실무 산출물 모드 가동을 위한 필수 유지보수"로 규정.

### 의제1 — P2-B Option B 채택
**evidence_mark_read.sh 패턴 확장 (Option B)** — 정상 OAuth/D0 작업에 대한 ok 증거 인정 패턴 추가.

**3가지 안전 조건 (양측 합의)**:
1. **물리 증거 한정**: 단순 스크립트명 매칭 절대 불가. 다음 2가지 중 하나만 인정:
   - stdout 명시적 성공 텍스트 (예: `OAuth.*200`, `로그인.*성공`)
   - 실행 결과로 생성되어야만 하는 특정 산출물 파일 존재 (토큰, xlsx 등)
2. **정규식 앵커링 강제 (Gemini 추가 위험)**: 시작 `^` + 끝 `$` 명시. 완전 일치(Exact Match) 강제. 부분 매칭 금지로 보안 우회 차단.
3. **수정 범위 제한**: 신규 hook 추가 금지 / settings 변경 금지 / evidence_gate 정책 변경 금지 / risk_profile_prompt 키워드 변경 금지. evidence_mark_read.sh 단일 파일 최소 보정만.

**TASKS.md 명시 의무**: "이번 수정이 마지막 시스템 개입임. 향후 P2-C/P3-E 재논의는 재논의 트리거(아래) 충족 시에만 허용"

### 의제2 — P2-C 동결 보류 (양측 만장일치)
**4종 모두 동결 보류**. settings 등록 금지, schtasks 등록 금지, 자동 호출 경로 부여 금지, 삭제도 금지. "보조 수동 도구 / 현재 운영 미사용" 상태 유지.

**세션77 합의와의 트레이드오프 명시**: "세션77 당시 안전망 부족이 핵심 리스크였으나, 현재는 안전망 과다가 병목. 상황 변화에 따른 합리적 보류이며 합의 무력화가 아닌 환경 적응."

### 의제3 — P3-E 동결 보류 (양측 만장일치)
**agents 7종 모두 동결 보류**. AGENTS_GUIDE 진입 추가 금지, 자동 호출 신설 금지, 삭제 금지, description 수정 금지. 필요 시 수동 호출만.

**재논의 트리거 명문화 (Gemini 신규 추가)**: "실무 산출물에 치명적 결함(예: 잘못된 정산 엑셀, 코드 포맷 파괴)이 발생하고, 해당 agent(artifact-validator 등)의 부재가 직접 원인으로 증명된 경우에 한해 재논의 허용"

## claude_delta 자체 선언
- **claude_delta: major**

  6-0 독자 답안과의 차이:
  - 의제1: Option B+C 결합 → **Option B 단독 + 정규식 앵커링 강제** (양측 합의로 C 배제 + 보안 강화)
  - 의제2: 운영 경로 부여 (폐기 0) → **동결 보류** (세션108 합의 일관성 우선)
  - 의제3: AGENTS_GUIDE 명문화 + 자동 호출 → **동결 보류 + 재논의 트리거 명문화** (시스템 안정 동결)

  Claude 단독 6-0 답안이 모두 시스템 개선 방향이었으나, 양측 검증을 거쳐 세션108 합의 일관성으로 수렴. **3-way 토론의 정합성 가치 실증**.

## issue_class
- **issue_class: B** — P2-B Option B 구현은 evidence_mark_read.sh 코드 변경으로 실행 흐름 변경. B 분류 (구조 변경 영역). 6-5 생략 불가.

## skip_65
- **skip_65: false** — B 분류이므로 양측 최종 검증 강제

## 자동 게이트 점검 (cross_verification 4키)
| 키 | 값 | 근거 |
|---|---|---|
| gpt_verifies_gemini | 동의 | Step 6-4 GPT 답: 4가지 핵심 모두 인정 |
| gemini_verifies_gpt | 동의 | Step 6-2 Gemini 답: 해석 C 세션108 원칙 완벽 부합 |
| gpt_verifies_claude | 대기 | 본 종합안 양측 검증에서 수령 |
| gemini_verifies_claude | 대기 | 본 종합안 양측 검증에서 수령 |

## pass_ratio (Step 6-5 종합 후 재계산)
- 현재 (Step 6-2/6-4 만): 2/2 = 100%
- Step 6-5 양측 검증 후 4/4 도달 시 채택 확정 (≥ 2/3)

## 다음 단계
1. **Step 6-5 양측 동시 검증**: 본 종합안을 GPT·Gemini 양측에 동시 전송, 1줄 검증 수령
2. **Step 5 구현**: P2-B Option B만 — evidence_mark_read.sh 단일 파일에 OAuth 정상 신호 패턴 + 정규식 앵커링 추가
3. **Step 5 양측 동시 최종 검증**: 커밋·push 후 GPT·Gemini 양측에 검증 요청 (단일 모델 단독 통과 금지)
