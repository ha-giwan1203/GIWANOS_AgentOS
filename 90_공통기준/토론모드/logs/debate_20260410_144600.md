# 토론 로그: 에이전트 운영 체계 개선 3건 설계 검토

- 세션: 2026-04-10 14:46 KST
- 채팅방: g-p-69bca228f9288191869d502d2056062c (프로젝트 최상단 방)
- 라운드: 2

## 의제

1. PROPER_NOUN_ALLOWLIST 구조 개선 (외부 파일 분리)
2. 훅 로그 JSON 스키마 통일
3. Incident Ledger 자동 연동

## 라운드 1: Claude → GPT

Claude가 3건 의제를 전송. 각 의제별 현황/방향/검토 요청 포함.

### GPT 응답 (라운드 1)

- **의제 1: 채택** — 코어 목록 코드 유지 + 외부 파일 가산형 merge + 로드 실패 시 코어만 사용
- **의제 2: 버림** — hook_common.sh의 hook_log()가 이미 JSON 출력({ts, event, msg}). 텍스트→JSON 전환 의제 자체가 실물과 불일치. 대안으로 스키마 확장 제안
- **의제 3: 채택(범위 수정)** — hook_incident() 이미 존재, auto_compile/completion_gate 이미 연동. 나머지 deny 훅 통일 + incident_ledger.jsonl 보존 정책 분리가 핵심

## 실물 검증

Claude 독립 검증 결과 GPT 주장 3건 모두 **사실**:
- hook_log() → hook_common.sh 50줄, JSON 출력 확인
- hook_incident() → hook_common.sh 158-174줄, 존재 확인, _rotate_file 적용 확인
- auto_compile.sh 16줄 compile_fail 자동 기록, completion_gate.sh 20/48줄 gate_reject 자동 기록

## 라운드 2: Claude 하네스 분석 → GPT 동의

### Claude 하네스 분석
- 채택: 2건 (의제 1 + 의제 3 범위 수정)
- 버림: 1건 (의제 2 — 사전조사 오류)
- 보류: 의제 2 대안 스키마 확장 1건

### 반론
- 의제 2 대안 스키마 확장은 보류: 현재 3필드로 운영 문제 미관측, 확장 시 호출부 전수 수정 필요

### GPT 응답 (라운드 2)
- 동의. 우선순위 확인.

## 합의안

| 순위 | 작업 | 판정 |
|------|------|------|
| 1순위 | allowlist 외부 파일 분리 | 채택 |
| 2순위 | incident ledger 보존 정책 분리 (_rotate_file 제외) | 채택(범위수정) |
| 3순위 | 나머지 deny 훅 incident 연동 | 채택(범위수정) |
| 보류 | hook_log 스키마 확장 | 보류 |
| 폐기 | 훅 로그 텍스트→JSON 전환 | 버림 (이미 JSON) |

## 미합의 쟁점
- 없음
