# 토론 세션 54 — 클로드코드 정밀분석

- 일시: 2026-04-17 KST
- 채팅방: `클로드코드 분석` (프로젝트방 내 최신)
- chat_url: https://chatgpt.com/g/g-p-69bca228f9288191869d502d2056062c-gpt-keulrodeu-eobmu-jadonghwa-toron/c/69e202a2-2c98-83e8-96d4-85e9fe5fd425
- 결과: **통과 ✅** (commit `6135692a`)

## 의제
"클로드코드 정밀분석" — 저장소 훅/게이트/테스트 전반 구조 점검과 취약점 도출.

## 턴별 요약

### 턴1: GPT 진단 (사용자 요청)
GPT 결론: 구조 상급이나 JSON 파싱·트랜스크립트 판정·문서/테스트 드리프트 약점.
- 문제1: safe_json_get 실파서 아님 (sed/tr 기반)
- 문제2·3: state_rebind_check / session_start_restore 쓰기훅 주입
- 문제4: final_check grep/sed 문자열 매칭
- 문제5: harness_gate 키워드 매칭 + `"decision":"block"` 잔재
- 문제6: smoke_test `27개` vs README `28개` 드리프트
- 문제7: completion_gate mtime 의존
- 즉시 손봐야 할 3개: safe_json_get 교체, harness_gate block→deny, smoke_test 27→28

### 턴2: Claude 하네스 분석 (채택 3 / 보류 3 / 버림 0)
**채택 (실물 검증됨)**
1. smoke_test 27 vs README 28 드리프트 — smoke_test.sh:528 `grep -q '27개' README.md` vs README.md:6 `28개 스크립트`. 커밋 `b2f47806` 불완전 마감 확인.
2. harness_gate `decision:block` 잔재 — harness_gate.sh:74 이미 `deny`, :118만 `block` 잔존. 파일 내부 포맷 불일치.
3. safe_json_get 실파서 아님 — hook_common.sh:84-112 sed/tr 3단 fallback, dot path 미해석. TASKS 기등록.

**보류 (일반론 or 효용 미정)**
- final_check grep/sed 파싱 — 포맷 강제로 대응 중, 전면 교체 효용 미정
- harness_gate 키워드 매칭 — 설계 한계 맞으나 교체 설계 없이 건드리면 게이트 흔들림
- completion_gate mtime 의존 — CLAUDE.md에서 이미 "최소 게이트" 명시, 재지적은 과잉

**독립 견해**: #1·#2는 커밋 `b2f47806`의 README-only 수정 부작용. fast smoke(9/9 PASS)만 돌려서 full section 27·30 실패 상태 노출 안 됨. #3은 별도 트랙.

### 턴3: GPT P0/P1 수정안
- P0-1: smoke_test.sh:528 `'27개'` → `'28개'` (부수효과 smoke_test+final_check+개수 literal grep)
- P0-2: harness_gate.sh:118 `"decision":"block"` → `"decision":"deny"` (부수효과 bash -n + block 잔재 grep + smoke_test)
- P1 (중복/기등록): hook_common.sh:84-112 safe_json_get 교체 — 승격 조건 명시
  - ①navigate/evidence/completion_gate 중 JSON 파싱 실패 incident 1회
  - ②중첩키 빈값 재현
  - ③7일 내 JSON 파싱 incident 2회 누적
- 스코프 경계: final_check/completion_gate/session_start_restore/state_rebind_check/evidence_gate/evidence_stop_guard/hook_config.json/README.md/TASKS/HANDOFF/STATUS 건드리지 않음

### 턴4: Claude 구현 + 커밋 + 푸시
- Edit: smoke_test.sh:528, harness_gate.sh:118 (각 1줄)
- TASKS/HANDOFF 세션54 기록 + safe_json_get 승격 조건 반영
- smoke_test 167/167 ALL PASS (regression 27~30 섹션 포함 전체 통과)
- bash -n harness_gate.sh OK / bash -n smoke_test.sh OK
- commit: `6135692a` / push: main

### 턴5: GPT 조건부 통과
사유: 커밋 실물 반영 확인됐으나 smoke_test 실행 출력/bash -n 결과를 GPT가 직접 재현 못 함.
부가 지적: completion_gate.sh의 `result":"block"` 잔재 2건 — 판정 영향 없음, `decision` 아닌 감사 로그 `result` 필드라 범위 구분 타당.

### 턴6: Claude 실행 실물 증거 보강 + 재판정 요청
- smoke_test tail 20줄 전문 + `ALL PASS` 확인
- bash -n 양쪽 OK
- completion_gate result:block은 의도적 감사 로그 네이밍임을 소스 라인 38/79 지목 설명
- 27개 literal 잔재 재확인 (매치 없음)

### 턴7: GPT 통과 ✅
"Git 실물 반영 + 실행 증거 첨부 + 범위 구분 타당 → 조건부 통과 사유 해소."
"세션54 완료 항목 유지, safe_json_get은 승격 조건 충족 시에만 P0 승격."

## 변경 파일
- `.claude/hooks/smoke_test.sh` (line 528)
- `.claude/hooks/harness_gate.sh` (line 118)
- `90_공통기준/업무관리/TASKS.md`
- `90_공통기준/업무관리/HANDOFF.md`

## 합의안
1. P0 2건 즉시 반영 — 완료
2. safe_json_get 교체는 별도 트랙, 승격 조건 3항 명시 — 반영 완료
3. 보류 3건(final_check/harness_gate 키워드/completion_gate mtime)은 설계 의도로 존속

## 미합의 쟁점
없음.

## 잔여 이슈
- safe_json_get 실파서 교체 — 승격 조건 충족 시에만 P0 승격 (현재 incident 미발견 → 후순위 유지)
