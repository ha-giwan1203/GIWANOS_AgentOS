# Round 1 — GPT 본론 (의제 1+2)

> 모델: gpt-5-thinking · 응답 길이: 6933자 · 수신 시각: 2026-04-27 19:0X KST

## 결론
기존 5종 유지. 추가 모드 0개. 분류 재구성 불필요. 단, 보조 태그 3개(리서치/학습, 상태정리, 회고/메타검토)를 A~E에 붙이는 방식으로 보강.

## 의제 1 — 모드 5종 분류의 완전성 (요약)

1. **5종은 1차 분류로 충분** [실증됨]: 도메인 진입표는 있으나 작업 성격 판정 계층이 없는 게 진짜 문제. 모드를 더 늘리면 Claude가 "분류표 맞추는 봇"이 됨.
2. **학습/리서치는 수단, 목적 귀속** [일반론]: 엑셀 자동화→A, hook 이해→B, CLAUDE.md 근거→C, GPT 비교→D, OAuth 장애→E.
3. **메모리/HANDOFF/TASKS 정리는 후처리** [실증됨]: 상태 원본 우선순위(TASKS>HANDOFF>STATUS)가 이미 정해져 있음. 독립 모드 만들면 산출물보다 문서 갱신을 먼저 보는 부작용.
4. **회고/메타는 B 또는 C로 흡수** [일반론]: 단순 진단이면 B, 규칙·문서 변경까지 가면 C. 회고 모드 신설은 메타순환 위험.
5. **외부 도구 설정(NotebookLM, Gemini bridge 등)은 C 또는 D로 귀속** [실증됨]: 명령어·registry·bridge·health script 추가는 시스템 구조 변경. 외부 모델 의견 묻기 자체는 D.

## 의제 2 — 모드 경계의 모호성 (요약)

1. **A↔C 경계 = 산출물 성격** [일반론]: A는 사용자가 현장에서 바로 쓰는 결과물(엑셀/대책서/MES업로드/정산결과). C는 Claude Code 작동 방식 변경(hook/settings/CLAUDE.md/SKILL.md/command/agent/gate).
2. **A 작업 중 시스템 파일 건드리면 즉시 C 승격** [실증됨]: 경로 기준 명시 (`.claude/hooks/`, `.claude/settings*.json`, `.claude/commands/`, `.claude/agents/`, `90_공통기준/스킬/*/SKILL.md`, 루트 CLAUDE.md, 도메인 CLAUDE.md). 단, 작업 종료 후 TASKS/HANDOFF 단순 갱신은 후처리.
3. **B↔E 경계 = "현재 실무가 막혔는가"** [일반론]: B는 시간 있는 진단(왜 evidence_gate 울리는가, 왜 hook 중복인가). E는 정상 복귀 우선(MES 업로드 실패, OAuth 실패, ERP 멈춤).
4. **E는 최소 복구까지만, 구조 개선은 B/C로 분리** [실증됨]: 세션108 이후 시스템 개선 모드 중단. E에서 재발방지 같이 처리하면 "장애 복구"가 "시스템 개선 우회로"가 됨.
5. **D는 사용자 명시 호출일 때만** [실증됨]: 자동 승격 차단해야 실무 흐름 보호.

## 우선순위 규칙 (GPT 제안)

1. **명시 지시 우선** [일반론] — "토론모드"/"분석만"/"수정 진행"/"대책서 만들어"/"업로드 살려라" 등 사용자 표현
2. **파일 경로가 시스템 영역이면 C 우선** [실증됨] — hook/settings/CLAUDE.md 포함 시 실무 목적이라도 C
3. **현재 실무 차단이면 E > B** [일반론] — 정상 복귀가 먼저, 재발방지는 사후 B
4. **D는 C보다 자동 우선하지 않음** [구현경로미정] — 자동 승격 기본 차단, 보완은 "C에서 R1~R5 + Plan 승인 강제"

GPT의 우선순위 사다리: **명시 > C(시스템 경로) > E > D > B > A**

## 반대안 검토 (GPT)

- **F. 리서치/학습 모드 신설** → 버림 (목적 아닌 수단, 모드 겹침)
- **F. 상태관리 모드 신설** → 버림 (문서 갱신을 작업 목적화하는 부작용)
- **F. 회고/메타 모드 신설** → 버림 (메타순환 가장 위험)
- **A를 실무 산출물 vs 실무 실행으로 분리** → 기존 5종 유지 (A 내부에 "ERP/MES 비가역 실행은 대상/일자/라인/건수 사전 확인" 한 줄 추가)

## 최종 결론 (GPT)
기존 5종 유지. 추가 모드 0개. 분류 재구성 불필요. 보강 6개 항목 채택 권고:
- 보조 태그 3개 (독립 모드 X)
- A↔C: 현장 산출물 vs 시스템 변경
- B↔E: 현재 실무 막힘 여부
- 시스템 파일 수정 포함 시 C 우선
- 실무 차단 복구는 E > B
- D 자동 승격 기본 차단, 명시 호출만
- E는 최소 복구까지만, 재발방지는 B/C 분리
