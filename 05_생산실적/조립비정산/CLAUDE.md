# 조립비 정산 작업 규칙

## 기본 원칙
- 대상: 대원테크 (업체코드 0109)
- 3월 정산: GERP 원본금액이 권위값 (2026-04-05 전환). 4월부터 기준정보 단가 기준으로 복귀 예정
- 계산 결과는 근거와 함께 보고한다
- DB 구조를 먼저 파악한 뒤 수정한다

## 수식 vs Python 분리
- 기존 DB: VLOOKUP 수식 기반 (수식 보존 필수, data_only=False)
- 신규 구조: Python이 전체 계산 → 값만 엑셀에 기록 (수식 없음)
- 두 구조를 혼용하지 않는다

## 정산 기준 체계

### ERP별 역할
- GERP: 최종 정산 기준. 주야구분 컬럼 값 사용
- 구ERP: 비교/검증용 참고 데이터. 최종 정산금액 기준 아님

### GERP 주야 판정
- 정상 = 주간
- 추가 = 야간
- "추가"는 별도 컬럼이 아니라 주야구분 컬럼의 값임

### 구ERP 주야 판정
- LOT B = 야간
- LOT B 이외 전부(A, C, S, R 등) = 주간
- 구ERP SD9 집계: 업체코드 0109 전체 품번 대상, 라인코드 무시

### SD9 야간 정산
- 총 정산금액 = (총 생산수량 × 기준단가) + (야간 생산수량 × 기준단가 × 30%)
- 30%는 야간수당 (야간 기본단가 30%가 아님)
- 즉, 전체에 100% 적용 후 야간분에 30% 추가 가산

### SP3M3 구ERP 기준
- SP3M3 구ERP는 모듈품번(MO)으로만 집계 → 라인별 피벗 매칭 불가
- **전체업체 피벗(all_day/all_night)으로 매칭** (SUB와 동일 방식)
- 구ERP 금액 = 구ERP수량 × 기준단가
- RSP 모듈품번 역추적과 혼동 금지 (별개 프로세스)
- SP3M3 야간: GERP 원본 야간금액 직접 사용 (3월 기준. 4월부터 기준단가 복귀 시 재설정)
- **SP3M3 구ERP 야간 = GERP 야간 동일 적용** (구ERP 서브라인 야간 구분 불가)
- **SP3M3 구ERP 주간 = 구ERP 총수량 - GERP 야간수량** (LOT B 주야 분류 무의미, 총수량 기준)

### 유형 분류 기준표
| 유형 | 조건 |
|------|------|
| GERP누락 | GERP 실적 없고 구ERP에만 있음 |
| 구실적누락 | 구ERP 실적 없고 GERP에만 있음 |
| 기준누락 | 기준정보 단가 없는데 GERP 실적 있음 |
| 다중단가 | 동일품번 기준정보에 2개+ 단가 |
| 단가+수량 | 단가·수량 모두 다름 |
| 단가차이 | 기준단가 ≠ GERP단가 |
| 수량차이 | 수량만 다름 |
| 정산차이 | 수량·단가 동일, 금액만 다름 (야간계산방식 차이 등) |

### SP3M3 야간 모듈품번(RSP) 역변환
- GERP SP3M3 야간("추가")행의 품번은 RSP 모듈품번(RSP3SC0697 등)으로 기록됨
- RSP 품번 = 야간 추가수당. GERP 추가행의 단가·금액이 곧 야간금액
- 매핑 파일: 1차 `03_품번관리/초물관리/SP3M3_모듈품번_최신.xlsx` + 2차 `10_라인배치/라인배치_ENDPART라인배정.xlsx`
- **RSP 처리는 step2에서만 수행**: RSP → 기본품번(10자리, 컬러코드 없음) 자동 변환
- step5에서 기준정보 품번(13자리) 앞 10자리로 매칭
- 같은 기본품번의 컬러별 단가는 동일 → 첫 번째 품번에 야간수량 합산 할당
- 매핑 미존재 RSP → GERP 원본금액 그대로 야간금액 적용 (역추적 포기)
- 모듈품번 파일에 RSP 추가 시 정상 매칭 흐름으로 자동 전환
- step4에도 RSP 역추적 코드가 있으나, 기준정보 파일 col4=Usage(숫자형)이므로 RSP 조건 불충족 → rsp_map 항상 빈 dict (dead code, 파이프라인 결과 영향 없음)

## SD9A01 단가기준판정 규칙
- SD9A01 단가기준판정: GERP단가 <=500 → 야간가산, >500 → 기본
- SD9A01에만 적용, 타 라인 적용 금지
- 적용 대상: SD9A01 시트 단가기준판정 컬럼(col 22)
- 야간실적 있는 행에만 입력 (실적=0 행은 None 유지)

## 기준정보 중복 처리 (2026-03-27 추가)
- 동일품번-동일단가 중복: step4에서 자동 제거 (첫 행 유지)
- 동일품번-다중단가: 정상 — 행별 개별 계산 후 합산

## 야간 표시 규칙 (2026-03-27 추가)
- GERP '정상' 행은 전체(주간+야간) 수량 포함
- GERP '추가' 행은 야간분
- 정산집계 표시: 주간수량 = 정상수량 - 추가수량, 야간금액 = 기본(100%) + 가산
- 금액 분리는 품번별 야간수량×기준단가로 계산 (단순 비례 아님)
- 합계 = 주간금액 + 야간금액 (불변)

## 조립품번 규칙 (2026-03-27 추가)
- 기준정보 D열(col3)에 조립품번 있음 (형식: `ANAAS04-01040`)
- GERP col11에도 조립품번 있음 (형식: `ANAAS0941`, 형식 다름)
- 파이프라인: 기준정보 조립품번 우선, 없으면 GERP 조립품번 fallback
- 매핑 키: (라인, 품번, 단가) → 조립품번
- 보고서 전 라인 상세시트에 조립품번 컬럼 표시
- 동일품번 다중단가 → 조립품번으로 구분 가능

## GERP 단가 표시 규칙 (2026-03-27 추가)
- GERP에 동일품번 다중단가 존재 가능 (조립품번별로 다른 단가)
- 보고서: 기준단가와 일치하는 GERP단가를 표시
- 일치하는 단가 없으면 → 단일단가면 그 단가, 다중이면 최소값 + 경고색

## 유형 판정 규칙
> 유형 분류 기준표(위 표)와 동일. 중복 표 제거됨 (2026-04-06 통합)

## 기타 비즈니스 규칙
- Usage=2 품번: 수량 2배 환산
- 구ERP는 전 업체 데이터가 필요 (SUB 라인 + SP3M3 매칭)
- ISAMS03 시트명: DB에서 '이너센스ASSY'

## 10개 라인
SD9A01, SP3M3, ANAAS04, DRAAS11, HASMS02, HCAMS02, WAMAS01, WABAS01, WASAS01, ISAMS03

## 관련 스킬
- assembly-cost-settlement: 정산 자동화 (`/settlement MM`으로 실행)

## 관련 에이전트 (도메인 지식 vs 실물 검증)

| 에이전트 | 역할 | 호출 조건 |
|---------|------|---------|
| `settlement-domain-expert` | 도메인 지식 질의 (NotebookLM 조립비정산_대원테크) | 규칙·공식·Known Exception·파이프라인 계약·용어 질문 |
| `settlement-validator` | 실물 데이터 검증 (정산결과 xlsx 대조) | `run_settlement_pipeline.py` 실행 후 결과 검증, `/settlement-validate` |

- 역할 분리 원칙 (세션55 영상분석 패턴): **에이전트=도메인 지식, 스킬=실행 레시피**
- NotebookLM 응답은 저장소 원본이 권위. 불일치 시 저장소 우선

## 월별 정산 실행 절차

### 원스텝 실행
```
/settlement 03      # 3월 정산 (환경세팅 → step1~8 → 검증보고)
/settlement 03 --start-from 5  # step5부터 재실행
```

### 수동 실행 (단계별)
```bash
# 1. 환경 세팅
python setup_month.py 03         # 폴더 생성 + 파일 복사 + config 수정
python setup_month.py 03 --dry-run  # 사전 확인

# 2. 파이프라인 실행 (step 1~8)
python run_settlement_pipeline.py
python run_settlement_pipeline.py --start-from 5 --use-cache  # 재시작

# 3. 산출물 확인
#   {월별폴더}/정산결과_MM월.xlsx   — 정산 보고서 (13개 시트)
#   {월별폴더}/오류리스트_MM월.xlsx — DB 양식 오류 리스트
```

### 파이프라인 8단계
| Step | 스크립트 | 출력 |
|------|---------|------|
| 1 | step1_파일검증.py | step1_validation.json |
| 2 | step2_GERP처리.py | step2_gerp.json |
| 3 | step3_구erp처리.py | step3_olderp.json |
| 4 | step4_기준정보매칭.py | step4_matched.json |
| 5 | step5_정산계산.py | step5_settlement.json |
| 6 | step6_검증.py | step6_validation.json |
| 7 | step7_보고서.py | 정산결과_MM월.xlsx |
| 8 | step8_오류리스트.py | 오류리스트_MM월.xlsx |

## 하네스 Evaluator 판정 기준표 (파일럿 — 2026-03-30 도입)

### HARD GATE (하나라도 실패 시 즉시 FAIL)
- 파이프라인 실행 성공 (Traceback 없음)
- 필수 시트 생성 완료 (10개 라인 시트 존재)
- 필수 컬럼 누락 없음

### SCORE 기준 (100점 만점)
| 항목 | 가중치 | 합격 기준 |
|------|--------|---------|
| 합계 정합성 | 40 | 원칙 0원 오차. STATUS.md 등록 반올림 예외는 INFO 처리 |
| 미매칭 건수 | 25 | 신규 미매칭 0건. STATUS.md 등록 기존 예외는 감점만 |
| 구조 정합성 | 20 | 필수 컬럼/시트/라인 누락 없음 |
| 금액 이상치 | 10 | 단가 ±10% 초과 없음 |
| 실행 안정성 | 5 | 예외 없이 완료, 산출물 정상 생성 |

### Known Exception 처리
- STATUS.md에 등록된 기존 예외 (SP3M3 -24원, RSP 미매칭 4건, 미매핑 품번 54건 등): 즉시 FAIL 아님, 감점 또는 INFO 처리
- 신규 미매칭 증가 또는 신규 구조 누락: FAIL

### 최종 판정
| 판정 | 기준 |
|------|------|
| PASS | HARD GATE 통과 + 80점 이상 |
| CONDITIONAL | HARD GATE 통과 + 60~79점 |
| FAIL | HARD GATE 실패 또는 60점 미만 |
| BLOCKED | 외부 데이터 미존재, 동일 실패 2회 반복. HANDOFF.md에 원인·재개위치 기록 |

### Evaluator 운영 원칙
- Evaluator는 Generator(파이프라인 실행)와 **별도 컨텍스트**에서 실행한다
- 재작업 시 이전 대화 전체를 다시 먹이지 않고, Evaluator 피드백만 전달한다
- 최대 3회 재작업 허용. 동일 실패 2회 반복 시 BLOCKED 전환

## 빈번 조회 데이터

| 항목 | 값 | 비고 |
|------|-----|------|
| 업체코드 | 0109 | 대원테크 |
| 라인 수 | 10개 | SD9A01, SP3M3, ANAAS04, DRAAS11, HASMS02, HCAMS02, WAMAS01, WABAS01, WASAS01, ISAMS03 |
| 기준정보 파일 | `01_기준정보/기준정보_라인별정리_최종_V1_20260316.xlsx` | 단가 권위값 |
| 파이프라인 진입점 | `03_정산자동화/run_settlement_pipeline.py` | step1~8 순차 실행 |
| 환경 세팅 | `03_정산자동화/setup_month.py` | 월 전환 자동화 |
| 대시보드 출력 | `_cache/월간_조립비_대시보드.png` | step7 시각화 |
| Slack 보고 | `03_정산자동화/step7_slack_보고.py` | PNG 첨부 (files:write scope 필요) |
| SP3M3 야간 단가 | GERP 원본금액 (3월), 4월부터 기준단가 복귀 | |
| SD9A01 단가기준판정 | <=500 야간가산, >500 기본 | SD9A01만 적용 |
| 야간수당 | 기준단가 × 30% 추가 가산 | 전체 100% + 야간 30% |

## 진행 현황
참조 오류 수치, 마감 월, 산출 파일명 등은 STATUS.md 참조
