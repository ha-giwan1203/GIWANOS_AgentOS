# 작업 노트 — 2026-04-22 ERP 생산계획 자동화 탐색

> 이 문서는 **내일(2026-04-23) 재개용 작업 메모**다.
> 상태 원본은 `90_공통기준/업무관리/TASKS.md`.

## 1. 오늘 범위

ERP 생산계획 반영 + SmartMES 서열정리 **일상 업무를 자동화**하기 위한 환경·API·스크립트 점검.
실제 저장/실행은 **오늘치 1건 수동 완료**(사용자 직접), 자동화 로직은 미완.

## 2. 확보한 정보

### 2.1 ERP 접속·로그인 (자동화 완료)
- Chrome 디버깅 모드: `--remote-debugging-port=9222`, 프로필 `C:\Users\User\.flow-chrome-debug`
- ERP URL: `http://erp-dev.samsong.com:19100/layout/layout.do` (루트 접속 시 OAuth 리다이렉트)
- OAuth URL: `http://auth-dev.samsong.com:18100/login`
- 자동 로그인: Chrome 저장 자격증명(`0109`) → pyautogui로 `userId` 필드 클릭 → ↓ → Enter → submit
- 스크립트: `.claude/tmp/erp_oauth_login.py` — 시스템 선택창 통과 후 ERP `/layout/layout.do` 진입까지 자동
- 참조: `90_공통기준/스킬/production-result-upload/SKILL.md` (MES용 동일 패턴)

### 2.2 D0추가생산지시 (부분 완료)
- URL: `http://erp-dev.samsong.com:19100/prdtPlanMng/viewListDoAddnPrdtPlanInstrMngNew.do`
- 경로: 생산관리 > 생산계획관리 > D0추가생산지시
- 엑셀 업로드 **팝업은 드래그 드롭 UI** → Playwright로 직접 파일 전송은 복잡 → API 호출로 우회
- iframe: `ifr_user` src=`popupPmD0AddnUpload.do?callbackFunid=totGridList`
- JS 소스: `/js/workspace/pm/prdtPlanMng/popupPmD0AddnUpload.js`

**API 구조 (JS 소스 분석 확인)**

| 단계 | 메서드 | URL | 방식 |
|------|--------|-----|------|
| 파싱(그리드 채우기) | POST | `/prdtPlanMng/selectListPmD0AddnUpload.do` | multipart, form id=`uploadfrm`, 파일 필드명 **`files`** (복수) |
| 저장 | POST | `/prdtPlanMng/multiListPmD0AddnUpload.do` | JSON `{"excelList":[{COL1:생산일, COL2:제품번호, COL3:생산량, ...}], "ADDN_PRDT_REASON_CD":"002"}` |

- `ADDN_PRDT_REASON_CD = "002"` (등록사유: 신규추가수량)
- `fileHelper.allSave()`가 form 내부 hidden(`callbackFunid`, `dataList`, `ADDN_PRDT_REASON_CD`)을 FormData에 자동 포함
- 500 에러 경험: 필드명 `uploadFile`/`file`/`excel` 시도 실패 → `files`(복수)로 해결해야 함 (미테스트)
- 스크립트: `.claude/tmp/erp_d0_upload.py` — `files` 필드로 수정된 버전 (실업로드 미검증)

**오늘치 데이터**
- 엑셀: `C:\Users\User\Desktop\SSKR D+0 추가생산 Upload.xlsx` (26행, 생산일 2026-04-22)
- 저장 결과: ERP 상단 그리드 43행(기존 17 + 신규 26), REG_USR_ID=0109, ADDN_PRDT_REASON_CD=002 확인
- 저장 경로: **사용자 수작업** (브라우저 드래그 드롭). 자동화 미완.

### 2.3 SmartMES 서열정리 (부분 완료)
- 스크립트: `06_생산관리/서열정리/smartmes_reorder.py`
- 엑셀: 동일 (`SSKR D+0 추가생산 Upload.xlsx`)
- SmartMES API: `http://lmes-dev.samsong.com:19220/v2/prdt/schdl/list.api` (조회만 동작, save.api는 서버 500 → UI 클릭 불가피)
- 대상: SP3M3 라인, 오늘 날짜, `workStatusCd == "R"` (대기)

**오늘 패치 (미커밋)**
- `smartmes_reorder.py:137` 재정렬 필터 수정
  - 기존: `shiftsCd == "02" AND workStatusCd == "R"` → 오늘은 shift=01이라 0건 매칭 버그
  - 신규: `workStatusCd == "R"` only (shift 무관, 주간 작업분은 F/P라 자동 제외)

**실행 흐름**
1. `python smartmes_reorder.py --calibrate` — 보조 모니터에서 좌표 6개 측정 (최초 1회)
   - 1행 중앙 y / 2행 중앙 y(피치 계산용) / 품번 컬럼 x / ↑버튼 xy / ↓버튼 xy / 저장버튼 xy(기본 1265,108)
2. `python smartmes_reorder.py` — dry-run, 재정렬 계획 출력
3. `python smartmes_reorder.py --execute` — 실제 UI 자동화 실행
- 안전장치: `pyautogui.FAILSAFE=True` (마우스 좌상단 이동 시 즉시 중단), 실행 5초 카운트다운

**오늘 관찰**
- 첫 조회(수동 업로드 직후): Excel 16~26번이 SmartMES rank 33~43과 불일치 11건
- 10분 후 재조회: 이미 일치. plan 0건
- 추정: MES 서버가 ERP 저장분을 지연 반영하면서 자동 정렬했거나 사용자 수동 조정. **내가 서열을 조작한 이력 없음** (`--execute` 미실행, config 파일 부재로 실행 차단)

## 3. 내일 재개 체크리스트

### 3.1 D0 자동 업로드 검증
- [ ] 새 날짜 엑셀로 `.claude/tmp/erp_d0_upload.py` 실행 (파싱→그리드 채우기)
- [ ] `files` 필드명으로 500 해소 여부 확인
- [ ] 저장(`multiList`) 자동화 추가 — excelList 페이로드 구조 실물 확인
- [ ] **중복 저장 방지 가드** 필수 — 오늘같이 이미 저장된 날짜 감지 시 차단

### 3.2 SmartMES 서열정리 검증
- [ ] `smartmes_reorder.py --calibrate` — 좌표 6개 캘리브레이션 (보조 모니터 `LG FULL HD (2)`에서)
- [ ] 결과 `smartmes_reorder_config.json` 확인
- [ ] dry-run으로 plan 출력 확인
- [ ] `--execute` 시범 실행 (실 업무 상황 재현)
- [ ] 오늘 패치(shift 필터 제거) 테스트 후 커밋

### 3.3 감시 로거 재구현
- [ ] `.claude/tmp/erp_d0_monitor.py`가 0건 캡처한 원인 수정
- [ ] Playwright `page.context.new_cdp_session(page)` → `Network.enable` 직접 활성화
- [ ] iframe 내부 fetch도 잡히도록 event source 확장

### 3.4 최종 산출물 목표
- `90_공통기준/스킬/d0-production-plan/` 신규 스킬로 패키징
  - SKILL.md / run.py / REFERENCE.md 구조 (`sp3-production-plan` 패턴 참조)
  - 트리거: "D0 추가생산", "생산계획 반영", "D0 업로드"
- SmartMES 서열정리는 이 스킬의 후속 스텝으로 통합 or 별도 스킬 유지 결정

## 4. 보존 파일 (내일 재사용)

| 파일 | 용도 | 상태 |
|------|------|------|
| `.claude/tmp/erp_oauth_login.py` | ERP 자동 로그인 | 동작 확인 |
| `.claude/tmp/erp_d0_upload.py` | D0 엑셀 업로드 자동화 (files 필드 수정본) | 실업로드 미검증 |
| `.claude/tmp/erp_d0_monitor.py` | 네트워크 감시 | 캡처 실패 → 재구현 필요 |
| `.claude/tmp/verify_order.py` | Excel vs SmartMES R순서 대조 | 동작 확인 |
| `06_생산관리/서열정리/smartmes_reorder.py` | SmartMES UI 자동 재정렬 | shift 필터 패치됨, 미커밋·미캘리브레이션 |
| `99_임시수집/erp_menu_map_20260410.json` | ERP 메뉴 URL 맵 | 조회용 |

## 5. Chrome 디버깅 세션

- 프로필: `C:\Users\User\.flow-chrome-debug` (포트 9222)
- 현재 ERP 로그인 상태 유지 중 — 세션 종료 시 OAuth 재로그인 필요
- 닫지 않고 유지하면 내일 바로 CDP 연결 가능
