version: "3.8"

services:
  qdrant:
    image: qdrant/qdrant:v1.9.1
    container_name: velos_qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"         # REST & gRPC
      - "6334:6334"         # Web UI
    volumes:
      - ./qdrant_storage:/qdrant/storage
